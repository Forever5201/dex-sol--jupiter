# 🚀 **启动指南 - 两个系统**

## **快速识别**

### **您想启动哪个系统？**

```
┌─────────────────────────────────────────────────────┐
│  🤔 您想要什么？                                     │
└─────────────────────────────────────────────────────┘

   ┌─────────────────────┬──────────────────────┐
   │                     │                      │
   ▼                     ▼                      ▼
   
覆盖100+池子          超低延迟                两者都要
使用Jupiter API      微秒级响应              同时运行
简单易用              自主控制                
   │                     │                      │
   ▼                     ▼                      ▼
   
系统1: Jupiter API    系统2: Rust Cache      运行两个系统
📁 packages/          📁 rust-pool-cache/     
   jupiter-bot/
```

---

## **系统1：Jupiter API 套利** 🟦

### **特点**
- 使用Jupiter官方API
- 覆盖100+ DEX池子
- 支持闪电贷
- 延迟：50-200ms

### **启动命令**

```bash
# 方法1：进入目录启动
cd packages/jupiter-bot
pnpm install          # 首次需要
pnpm run flashloan-bot

# 方法2：使用启动脚本（项目根目录）
./start-flashloan-bot.bat
./start-flashloan-bot.sh

# 方法3：干运行模式（测试）
cd packages/jupiter-bot
pnpm run flashloan-dryrun
```

### **配置文件**
```
packages/jupiter-bot/my-config.toml
```

### **验证启动成功**
```
✅ 看到：
- "Jupiter bot started"
- "Subscribing to pools..."
- "Monitoring opportunities..."
- 定期输出套利机会

❌ 不应该看到：
- "Connection failed"
- "API key invalid"
```

---

## **系统2：Rust Pool Cache 自主监控** 🟧

### **特点**
- 直接从链上获取数据（不用Jupiter API）
- 监控27个核心池子
- 超低延迟（10-50微秒）
- 自主路由算法

### **启动命令** ⭐

```powershell
# 进入rust-pool-cache目录
cd rust-pool-cache

# 方法1：统一启动脚本（推荐）
.\START.ps1 fix        # Vault修复启动（当前推荐）
.\START.ps1            # 正常启动
.\START.ps1 test       # 测试启动（30秒）

# 方法2：简单启动
.\run.bat              # Windows
./run.sh               # Linux

# 方法3：直接用cargo
cargo run --release --bin solana-pool-cache
```

### **配置文件**
```
rust-pool-cache/config.toml
```

### **验证启动成功**
```
✅ 看到：
- "WebSocket connected"
- "Subscribed to 27 pools"
- "Subscribed to 13 vaults"
- "Price update stats: XXX total events"
- "Statistics - Last 60 seconds"
- "✅ Triggering arbitrage scan #N"

❌ 不应该看到（修复后）：
- "unknown subscription ID" (大量警告)
- "0 scans triggered" (持续为0)
- "13 pools never updated"
```

---

## **对比表格**

| 特性 | Jupiter API (系统1) | Rust Pool Cache (系统2) |
|------|-------------------|----------------------|
| **目录** | `packages/jupiter-bot/` | `rust-pool-cache/` |
| **启动** | `pnpm run flashloan-bot` | `.\START.ps1 fix` |
| **语言** | TypeScript | Rust |
| **数据源** | Jupiter API | Solana WebSocket |
| **延迟** | 50-200ms | 10-50μs |
| **池子数** | 100+ | 27 |
| **闪电贷** | ✅ 支持 | ❌ 不支持 |
| **依赖** | Jupiter服务 | 仅Solana RPC |
| **竞争** | 高 | 低 |
| **适合** | 大额、覆盖广 | 高频、低延迟 |

---

## **常见场景**

### **场景1：我想要最全面的覆盖**
```bash
cd packages/jupiter-bot
pnpm run flashloan-bot
```
→ 使用Jupiter API系统

### **场景2：我想要最低延迟**
```powershell
cd rust-pool-cache
.\START.ps1 fix
```
→ 使用Rust Pool Cache系统

### **场景3：我想两个都运行**

**终端1**（Jupiter API）:
```bash
cd packages/jupiter-bot
pnpm run flashloan-bot
```

**终端2**（Rust Cache）:
```powershell
cd rust-pool-cache
.\START.ps1 fix
```

**注意**: 
- 两个系统可以同时运行
- 它们使用不同的数据源，互不冲突
- 但需要确保足够的系统资源

---

## **快速检查：我启动了哪个系统？**

### **检查方法1：看目录**
```bash
pwd  # 或 cd (查看当前目录)

# 如果显示：
.../packages/jupiter-bot    → 系统1 (Jupiter API)
.../rust-pool-cache         → 系统2 (Rust Cache)
```

### **检查方法2：看进程**
```powershell
# Windows PowerShell
Get-Process | Select-String "node|solana-pool-cache"

# 如果看到：
node.exe            → 系统1 (Jupiter API)
solana-pool-cache   → 系统2 (Rust Cache)
```

### **检查方法3：看日志**
```
系统1 (Jupiter API):
- "Using Jupiter Quote API"
- "Fetching routes from Jupiter"
- "Flashloan enabled"

系统2 (Rust Cache):
- "WebSocket connected"
- "Borsh deserialization"
- "Price update stats"
- "Consistency score"
```

---

## **我应该用哪个？**

### **如果您是新手**
→ 先用 **Jupiter API**（系统1）
- 更简单
- 覆盖更全
- 社区支持好

```bash
cd packages/jupiter-bot
pnpm run flashloan-bot
```

### **如果您是高级用户**
→ 用 **Rust Pool Cache**（系统2）
- 更快
- 更灵活
- 竞争更少

```powershell
cd rust-pool-cache
.\START.ps1 fix
```

### **如果您追求极致**
→ **两个都用**
- Jupiter API: 捕捉长尾机会
- Rust Cache: 捕捉高频机会

---

## **当前修复状态**

### **Jupiter API 系统（系统1）**
✅ 完全正常工作
- 可以直接启动使用

### **Rust Pool Cache 系统（系统2）**
🔧 刚修复完成，需要测试
- 修复了13个池子Vault数据问题
- 需要运行 `.\START.ps1 fix` 验证效果

---

## **总结**

| 我想... | 启动命令 | 目录 |
|--------|---------|------|
| 使用Jupiter API | `pnpm run flashloan-bot` | `packages/jupiter-bot/` |
| 超低延迟监控 | `.\START.ps1 fix` | `rust-pool-cache/` |
| 测试Vault修复 | `.\START.ps1 fix` | `rust-pool-cache/` |

**回答您的问题**:

> `.\START.ps1 fix` 是启动Jupiter官API套利的吗？

**答案**: ❌ **不是！**

`.\START.ps1 fix` 启动的是 **Rust Pool Cache**（系统2），这是一个**不使用Jupiter API**的自主监控系统。

如果您想启动 **Jupiter API 套利**，应该运行：
```bash
cd packages/jupiter-bot
pnpm run flashloan-bot
```

---

📖 **更多信息**:
- `rust-pool-cache/系统架构说明.md` - Rust系统详细说明
- `项目系统对比.md` - 两个系统详细对比
- `rust-pool-cache/SIMPLE_START.md` - Rust系统快速开始



